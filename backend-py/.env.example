# Same names as backend/.env — see docs/creds.md. By default config.py
# also reads ../backend/.env, so this file is optional during migration.
DATABASE_URL=
DIRECT_URL=
JWT_SECRET=
JWT_EXPIRES_IN=7d
API_PORT=4001
AI_ENGINE_URL=http://localhost:8000
